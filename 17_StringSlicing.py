"""Program will slice the string into the following length"""
def slicing():
    """Input to string and length Outout to string that are cut in length"""
    character = input()
    length = int(input())
    print(character[:length])
slicing()
