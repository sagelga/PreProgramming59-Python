"""This program will ask input then, run the program, later then print out the results"""
def major_branch():
    """This is the main fuction. It will do everything."""
    message = input()
    up_message = ""
    low_message = ""
    while counter != len(message)
        if message[counter].isupper
            up_message += message[counter]
            counter += 1
        else:
            counter += 1
    counter = 0
    while counter != len(message)
        if message[counter].islower
            low_message += message[counter]
            counter += 1
        else:
            counter += 1
major_branch()
